{
  "name": "guauguau",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
		"wp-server": "browser-sync start --proxy localhost --startPath perros --files \"./**\" --reload-delay=300  --reload-debounce=500"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.8"
  }
}
